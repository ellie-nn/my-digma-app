<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://android.com"
    package="org.oldschool.digmarecorder"
    android:versionCode="102410004"
    android:versionName="1.0.4.1"> <!-- ВАША ПОБЕДНАЯ ПРОВЕРОЧНАЯ ВЕРСИЯ! -->

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="33" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:label="Digma Recorder"
        android:icon="@mipmap/icon"
        android:allowBackup="true"
        android:hardwareAccelerated="true">

        <activity
            android:label="Digma Recorder"
            android:name="org.kivy.android.PythonActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="fontScale|layoutDirection|density|smallestScreenSize|screenSize|uiMode|screenLayout|orientation|navigation|keyboardHidden|keyboard|touchscreen|locale|mnc|mcc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- НАШ ОФИЦИАЛЬНЫЙ СЛУЖЕБНЫЙ ПАСПОРТ JOOD -->
        <service
            android:name="org.oldschool.digmarecorder.DigmaJavaService"
            android:exported="false"
            android:process=":service"
            android:stopWithTask="false"/>

    </application>
</manifest>
